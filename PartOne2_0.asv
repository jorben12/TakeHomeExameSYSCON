clear all
close all

%z1 =[0+45i 0-45i];
%p1 =[1 1];

%z2 =  [0+500i 0-500i];
%p2 =  [2000 300000];

%k = 1;
%sys1 = zpk(z1,p1,k);
%sys2 = zpk(z2, p2, k);

sys1 = tf([1 0 50**2],[])
sys = sys2 * sys1;

bode(sys1)
hold on
bode(sys2)
hold on
bode(sys)
